import React from "react"
import "./header.scss"

function Header() {
    return (
        <header className="navbar">Welcome to the portal of current exchange rates</header>
    )
}

export default Header